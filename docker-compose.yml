version: "3"
services:
  redis:
    image: redis
    restart: always
  postgres:
    image: postgres
    environment:
      POSTGRES_USER: user
      POSTGRES_PASSWORD: pass
      POSTGRES_DB: outline
    volumes:
      - ./data/pgdata:/var/lib/postgresql/data
    restart: always
  minio:
    image: minio/minio
    volumes:
      - ./data/minio_root:/minio_root
    command: "minio server /minio_root"
    env_file: ./env.minio
    restart: always
    volumes:
      - ./data/certs:/root/.minio/certs
  outline:
    # TODO: remove hardcoded version
    image: outlinewiki/outline:version-0.43.3
    # command: sh -c "yarn build && yarn sequelize db:migrate && yarn start"
    command: sh -c "yarn sequelize db:migrate && yarn dev"
    environment:
      - DATABASE_URL=postgres://user:pass@postgres:5432/outline
      - DATABASE_URL_TEST=postgres://user:pass@postgres:5432/outline-test
      - REDIS_URL=redis://redis:6379
    env_file: ./env.outline
    restart: always
    depends_on:
      - postgres
      - redis
      - minio
  nginx:
    image: nginx
    ports:
      - 80:80
      - 443:443
    volumes:
      - ./data/nginx/:/etc/nginx/conf.d/
      - ./data/certs/:/etc/ssl/certs/
    restart: always
    depends_on:
      - outline
